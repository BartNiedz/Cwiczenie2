@model IEnumerable<Audiobooks>
<div class="container mt-2">
<div class="row container-fluid">
    @if (Model.Count() == 0)
    {
        <p>Nie znalezniono audiobookow w takiej kategorii</p>
    }
    else
    {
        foreach (var audiobooks in Model)
        {
            <div class="col-2 p-1" style="border-radius:5px; border: 1px solid black; background-color:antiquewhite; padding:0px;">
                <img class="img-fluid p-1" src="@audiobooks.simple_thumb" />

                <p class="text-center">@audiobooks.author</p>
                <p class="text-center">@audiobooks.genre</p>
                <p class="text-center">@audiobooks.title</p>
                <p class="text-center">@audiobooks.genre</p>
            </div>
        }
    }


</div>
</div>